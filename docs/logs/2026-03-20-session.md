# Session Log - 2026-03-20

## Summary

- Implemented backend dashboard briefing surface (`GET /api/dashboard/briefing`) with DB-backed aggregates:
  - recent topics
  - movement classification
  - latest run metadata
  - pipeline metrics
  - source health
- Added LLM summarizer integration via OpenAI Responses API for briefing bullets.
- Hardened summarizer reliability:
  - removed unsupported request parameter for `gpt-5-mini`
  - added retry/backoff for transient provider failures
  - increased default timeout to reduce false-negative fallbacks

## Decisions

- Use LLM-first briefing synthesis for dashboard bullets.
- Do not generate synthetic rules-based fallback bullets when LLM is unavailable.
- Return explicit unavailable metadata for frontend state handling:
  - `briefing_available`
  - `briefing_unavailable_reason`

## Verification

- Backend targeted tests passed:
  - `tests/test_dashboard_briefing_api.py`
  - `tests/test_candidates_api.py`
  - `tests/test_candidate_details_api.py`
- LLM smoke verification completed with real API key and network-enabled run.

## Docs Updates

- Updated backend plan to reflect final LLM-first and unavailable-state behavior:
  - `docs/plans/2026-03-20-dashboard-ai-briefing-backend-plan.md`
- Updated frontend refresh plan to include blurred unavailable-state UX contract:
  - `docs/plans/2026-03-20-frontend-refresh-plan.md`

## Next Step

- Implement frontend dashboard unavailable-state rendering (blur + message) using backend metadata.

## Frontend Refresh Progress

- Implemented shared shell and navigation-first IA:
  - `Dashboard`, `Settings`, `Run Console`, `Shortlist`
  - persistent primary navigation landmark (`aria-label="Primary"`)
- Added reusable UI primitives and applied them across the workflow:
  - `SectionCard`, `StatusBadge`, `EmptyState`, `LoadingPanel`, `StatMetric`
- Redesigned pages to match Discovery Desk workflow:
  - Home dashboard with `AI Briefing`, `Recent Topics`, `Pipeline Funnel`, `Operational Context`
  - Settings grouped into `Discovery profile`, `Target audience`, `Filtering and output`
  - Run Console with summary metrics and source execution board
  - Shortlist with filtering toolbar and curation actions
  - Topic details with `Score breakdown`, `Evidence`, `Content angles`
- Implemented AI briefing unavailable behavior:
  - keep card layout
  - blur content panel
  - show centered message `AI assessment is currently unavailable.`
  - no synthetic fallback briefing bullets
- Added diagnostics rendering for backend payload:
  - dashboard `drop_reasons` chips
  - source-health metrics in operational context
- Normalized frontend local API default:
  - `apps/web/src/lib/api.ts`: `http://127.0.0.1:8000`
  - `.env.example`: `VITE_API_BASE_URL=http://127.0.0.1:8000`
  - `docs/API_UI_USAGE_GUIDE.md` updated to `127.0.0.1`

## Frontend Verification

- `npm --prefix apps/web test` -> PASS (`4/4`)
- `npm --prefix apps/web run build` -> PASS
- Responsive Playwright pass executed for `375`, `768`, `1280` widths:
  - primary nav remains visible
  - dashboard unavailable mode is readable and stays in-card (blur + message)
  - route shells are visually consistent
  - explicit focus ring confirmed via keyboard tab navigation
- Note: table-overflow behavior in live browser could not be fully exercised in this pass because API was unavailable during visual QA, so shortlist/run data tables did not populate.
