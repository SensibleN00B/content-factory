# API

Python backend managed with `uv`.

## Quick start

- Install deps: `uv sync --project apps/api --group dev`
- Lint: `uv run --project apps/api ruff check .`
- Format: `uv run --project apps/api ruff format .`
- Test: `uv run --project apps/api pytest tests`
